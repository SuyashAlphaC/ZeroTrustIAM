# DEPRECATED: replaced by cnpg-cluster.yaml.
# Operator-managed cluster (CloudNativePG) provides streaming replication,
# automatic failover, continuous WAL archiving, PITR, and a writer/reader
# PgBouncer fronting (see pgbouncer.yaml, pgbouncer-readonly.yaml).
#
# This file is intentionally empty. It is no longer referenced from
# k8s/kustomization.yaml. Kept as a tombstone so reviewers searching for the
# legacy single-pod Deployment land here.
